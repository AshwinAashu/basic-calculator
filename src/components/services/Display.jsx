const Display = ({ char }) => {
  //needs to check if we are adding two operators
  //consequently

  // let calEquation = "".concat(char);
  console.log(char);
  return <div className="calculator-display">{char}</div>;
};
export default Display;
